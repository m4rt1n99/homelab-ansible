---
- name: Restart immich-postgres
  ansible.builtin.systemd:
    name: immich-postgres
    state: restarted

- name: Restart immich-redis
  ansible.builtin.systemd:
    name: immich-redis
    state: restarted

- name: Restart immich
  ansible.builtin.systemd:
    name: immich
    state: restarted
